imports:
  - { resource: 'config_development.yml' }

framework:
  test: ~
  session:
    storage_id: 'session.storage.mock_file'
  profiler:
    collect: false

web_profiler:
  toolbar: false
  intercept_redirects: false

swiftmailer:
  disable_delivery: true
